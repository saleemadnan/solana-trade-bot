{
  "name": "solana-trade-bot",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start:http": "node index.js",
    "start:websocket": "node websocket.js",
    "validate:config": "node scripts/validate-config.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check index.js && node --check websocket.js && npm test && node scripts/validate-config.js --example"
  },
  "author": "",
  "license": "MIT",
  "description": "Educational Solana trading bot proof of concept with dry-run safeguards and config validation.",
  "dependencies": {
    "@solana-tracker/data-api": "^0.0.5",
    "bs58": "^6.0.0",
    "chalk": "4.1.0",
    "dotenv": "^16.5.0",
    "solana-swap": "^1.1.6",
    "winston": "^3.17.0"
  }
}
